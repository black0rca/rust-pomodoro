# rust-pomodoro

##how to use just you need open the file pomodoro write this command into cli 

you can change numbers like wors 30m change to 25m is like that is simpale 

```shell
cargo run --release -- --work 30 --short 10 --long 20
```
